{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Mise à jour dossier</h1>
<hr>
    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('dossier') }}">
            Retour à la liste
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}

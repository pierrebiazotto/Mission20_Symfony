{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Sointechnique</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Coefficientacte</th>
                <td>{{ entity.coefficientacte }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('sointechnique') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('sointechnique_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}

{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Sointechnique list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Coefficientacte</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('sointechnique_show', { 'id': entity.id }) }}">{{ entity.coefficientacte }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('sointechnique_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('sointechnique_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('sointechnique_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}

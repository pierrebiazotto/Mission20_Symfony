{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Soinbase</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Tempestime</th>
                <td>{{ entity.tempestime }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('soinbase') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('soinbase_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}

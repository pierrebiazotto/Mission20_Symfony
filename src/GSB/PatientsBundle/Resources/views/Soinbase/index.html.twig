{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Soinbase list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Tempestime</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('soinbase_show', { 'id': entity.id }) }}">{{ entity.tempestime }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('soinbase_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('soinbase_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('soinbase_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}

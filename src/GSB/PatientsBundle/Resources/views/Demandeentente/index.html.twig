{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Liste des demande d'entente</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Date de demande</th>
                <th>Date de dfin</th>
                <th>Réponse</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('demandeentente_show', { 'id': entity.numdossier }) }}">{{ entity.datedemande }}</a></td>
                <td>{% if entity.datefin %}{{ entity.datefin }}{% endif %}</td>
                <td>{{ entity.reponse }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('demandeentente_show', { 'id': entity.numdossier }) }}">Montrer</a>
                    </li>
                    <li>
                        <a href="{{ path('demandeentente_edit', { 'id': entity.numdossier }) }}">Mettre à jour</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('demandeentente_new') }}">
                Créer une nouvelle ligne
            </a>
        </li>
    </ul> 
    {% endblock %}
